#[cfg(test)]
pub mod test_list {
    use crate::data_structures::list::LinkedList;

    #[test]
    fn test_list() {
        let mut _list = LinkedList::<i32>::new();
    }

    #[test]
    fn test_list_2() {
        let mut _list = LinkedList::<i32>::new();
    }
}

#[cfg(test)]
pub mod test_graph {
    use crate::data_structures::graph::Graph;

    #[test]
    fn test_graph() {
        let mut _graph = Graph::new();
    }
}
